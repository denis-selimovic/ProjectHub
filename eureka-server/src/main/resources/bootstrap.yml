spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      enabled: true

server:
  port: 8761

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  server:
    waitTimeInMsWhenSyncEmpty: 5
