spring:
  application:
    name: notification-service
  cloud:
    config:
      password: secret
      username: root
      discovery:
        enabled: true
        service-id: config-service
      enabled: true
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    password: password
    username: projecthub

server:
  port: 8084
