spring:
  datasource:
    url: jdbc:h2:mem:system-events-service-test
    username: projecthub
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false