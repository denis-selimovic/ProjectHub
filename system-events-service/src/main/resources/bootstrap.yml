spring:
  application:
    name: system-events-service
  cloud:
    config:
      password: secret
      username: root
      discovery:
        enabled: true
        service-id: config-service
      enabled: true
  profiles:
    active: dev

server:
  port: 8086