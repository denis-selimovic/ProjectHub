spring:
  application:
    name: task-service
  cloud:
    config:
      password: secret
      username: root
      discovery:
        enabled: true
        service-id: config-service
      enabled: true
  profiles:
    active: dev
  jpa:
    properties:
      hibernate:
        session_factory:
          interceptor: ba.unsa.etf.nwt.taskservice.utility.HibernateUtils
  rabbitmq:
    host: localhost
    password: password
    username: projecthub

server:
  port: 8083
