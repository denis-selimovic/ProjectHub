spring:
  application:
    name: user-service
  cloud:
    config:
      password: secret
      username: root
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    password: password
    username: projecthub


server:
  port: 8081

amqp:
  queue: user-service
  exchange: user-exchange
  routing-key: user-routing-key

