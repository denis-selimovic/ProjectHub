spring:
  application:
    name: user-service
  cloud:
    config:
      password: secret
      username: root
      enabled: true
      uri: ${CONFIG_SERVER_URL}
  profiles:
    active: dev
  rabbitmq:
    host: localhost
    password: password
    username: projecthub

eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVER_URL}
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8081
